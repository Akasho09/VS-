#include <iostream>
#include <vector>
using namespace std;
int main (){
cout << "HIIII" <<endl;

vector <int> v1;
vector<int> numbers = {10, 20, 30};

vector<int> name(10, 4);  // every element of the vector with the value

// Assign vector 
    vector<int> v; 
  
    // fill the vector with 10 five times 
    v.assign(5, 10); 

vector <int> last (name);  // vector that is an exact copy of other_vec.


}