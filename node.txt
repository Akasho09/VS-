Require is #include of node ,used to import modules.
express is framework for node
versioning :->{
    ^4.18.2
    2->minor fixes(optional)
    18->bug fix release (security fix ) recommended to install
    4->major/breaking release
    ^ ->restricts install function to 4 major release ie (4.0.0 <= version >5.0.0)
    ~ ->restricts install function in minor release ie (4.18.0 <= version >=4.18.9)
}
